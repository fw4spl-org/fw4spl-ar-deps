--- a/cmake/EigenTesting.cmake	2012-11-05 22:22:49.000000000 +0100
+++ b/cmake/EigenTesting.cmake	2015-10-01 09:21:16.000000000 +0200
@@ -373,7 +373,7 @@
 
 macro(ei_get_cxxflags VAR)
   set(${VAR} "")
-  ei_is_64bit_env(IS_64BIT_ENV)
+  # ei_is_64bit_env(IS_64BIT_ENV) crash in cross compiling
   if(EIGEN_TEST_NEON)
     set(${VAR} NEON)
   elseif(EIGEN_TEST_ALTIVEC)
@@ -420,7 +420,7 @@
     set(TMP_BUILD_STRING ${TMP_BUILD_STRING}-${LOCAL_COMPILER_FLAGS})
   endif()
 
-  ei_is_64bit_env(IS_64BIT_ENV)
+  # ei_is_64bit_env(IS_64BIT_ENV) crash in cross compiling
   if(NOT IS_64BIT_ENV)
     set(TMP_BUILD_STRING ${TMP_BUILD_STRING}-32bit)
   else()
